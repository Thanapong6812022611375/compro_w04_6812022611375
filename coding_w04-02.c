#include<stdio.h>                                        // เพิ่มเครื่องหมาย # // เพิ่มเครื่องหมาย<>

int main () {                                          // เพิ่มเครื่องหมาย ;
    int a = 10;                                            // เพิ่มเครื่องหมาย ; // เพิ่มเครื่องหมาย \n 
    printf("a = %d\n", a); 
    return 0;                                          // เพิ่มเครื่องหมาย ;  
}                                                